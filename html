<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Minimal Sandwich Builder</title>

<style>
    body {
        margin: 0;
        background: #f4f4f4;
        font-family: Arial, sans-serif;
        display: flex;
        flex-direction: column;
        min-height: 100vh;
    }

    header {
        background: #4CAF50;
        color: white;
        padding: 20px;
        font-size: 28px;
        font-weight: bold;
        text-align: center;
    }

    main {
        display: flex;
        gap: 20px;
        padding: 20px;
        flex-grow: 1;
    }

    /* Sidebar */
    .sidebar {
        width: 250px;
        padding: 20px;
        background: white;
        border-radius: 12px;
        box-shadow: 0 3px 10px rgba(0,0,0,0.1);
    }

    .sidebar h2 {
        margin-bottom: 15px;
        border-bottom: 2px solid #eee;
        padding-bottom: 8px;
        font-size: 20px;
    }

    .ingredient {
        background: #f2f2f2;
        padding: 10px;
        border-radius: 10px;
        text-align: center;
        cursor: pointer;
        font-size: 14px;
        border: 1px solid #ddd;
        margin-bottom: 10px;
    }

    .ingredient:hover {
        background: #e8e8e8;
    }

    /* Sandwich area */
    .sandwich-area {
        flex-grow: 1;
        background: white;
        border-radius: 12px;
        padding: 30px;
        box-shadow: 0 3px 10px rgba(0,0,0,0.1);
        display: flex;
        justify-content: center;
    }

    #sandwich {
        width: 250px;
        text-align: center;
    }

    .top-bun {
        background: #F5DEB3;
        height: 45px;
        border-radius: 20px 20px 5px 5px;
        width: 110%;
        margin: 0 auto 10px auto;
        box-shadow: 0 3px 5px rgba(0,0,0,0.15);
    }

    .bottom-bun {
        background: #D2B48C;
        height: 35px;
        border-radius: 0 0 20px 20px;
        width: 100%;
        margin: 10px auto 0 auto;
        box-shadow: 0 -3px 5px rgba(0,0,0,0.15);
    }

    .layer {
        height: 25px;
        width: 100%;
        border-radius: 8px;
        margin: 6px auto;
        box-shadow: 0 3px 5px rgba(0,0,0,0.1);
    }

    /* Reset button */
    #reset {
        margin-top: 20px;
        padding: 10px;
        width: 100%;
        background: #d9534f;
        color: white;
        border: none;
        border-radius: 8px;
        cursor: pointer;
        font-weight: bold;
    }

    #reset:hover {
        background: #c9302c;
    }
</style>
</head>
<body>

<header>🥪 Sandwich Builder (Minimal JS)</header>

<main>
    <!-- Ingredients Sidebar -->
    <div class="sidebar">
        <h2>Ingredients</h2>

        <div class="ingredient" data-color="#8BC34A">🥬 Lettuce</div>
        <div class="ingredient" data-color="#E57373">🍅 Tomato</div>
        <div class="ingredient" data-color="#FFEB3B">🧀 Cheese</div>
        <div class="ingredient" data-color="#D50000">🥓 Bacon</div>
        <div class="ingredient" data-color="#BCAAA4">🦃 Turkey</div>
        <div class="ingredient" data-color="#689F38">🥒 Pickles</div>

        <button id="reset">Reset</button>
    </div>

    <!-- Sandwich Visual -->
    <div class="sandwich-area">
        <div id="sandwich">
            <div class="top-bun"></div>
            <div id="layers"></div>
            <div class="bottom-bun"></div>
        </div>
    </div>
</main>

<script>
/* ---- Minimal JavaScript ---- */

const ingredients = document.querySelectorAll('.ingredient');
const layerBox = document.getElementById('layers');
const resetBtn = document.getElementById('reset');

ingredients.forEach(btn => {
    btn.addEventListener('click', () => {
        const color = btn.dataset.color;

        const layer = document.createElement('div');
        layer.className = 'layer';
        layer.style.background = color;

        layerBox.appendChild(layer);
    });
});

resetBtn.addEventListener('click', () => {
    layerBox.innerHTML = '';
});

</script>

</body>
</html>
